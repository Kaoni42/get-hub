# Hello World Script

This repository contains a simple Python script that prints a hello message and the current date and time.

## How to run

To run the script, use the following command:

```bash
python hello.py
```